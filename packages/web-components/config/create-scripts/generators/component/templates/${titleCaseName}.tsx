import React from 'react';
import styles from './${titleCaseName}.module.scss';

const ${titleCaseName} = () => {
  return (
    <div>
      ${titleCaseName} Works!
    </div>
  );
}
export default ${titleCaseName};
